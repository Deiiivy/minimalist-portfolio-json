---
import {basics} from '../cv.json'

const {name, label, image, email, phone} = basics
---

<section>
<h1>{name}</h1>
<p>{label}</p>
<img src={image} alt={name} />
</section>

<style>

section {
    max-width: 700px;
    margin: 0 auto 72px;
}
</style>
